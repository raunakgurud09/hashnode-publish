describe("run", () => {
  beforeEach(() => {
    // Clear all mock function calls and reset mock implementation
    jest.clearAllMocks();
  });

  test("passing test", () => {
    expect(true).toBe(true);
  });
});
